{
  "name": "portfolioo-ai-proxy",
  "version": "0.1.0",
  "description": "Simple proxy server to forward assistant requests to an AI provider (keep API keys server-side)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "helmet": "^6.0.0",
    "node-fetch": "^2.6.7"
  }
}
